# ბებოს რეცეპტები — Deploy (Railway + Vercel)

არქიტექტურა:
- **Backend (API + ბაზა)** → Railway-ზე (`server/` საქაღალდე, Express + Postgres)
- **Frontend (საიტი)** → Vercel-ზე (`index.html`)

საიტი მუშაობს **ლოკალურ რეჟიმშიც** (ბაზის გარეშე) — რეცეპტები ინახება ბრაუზერში.
საერთო ბაზისთვის (ყველა მოწყობილობაზე გამოჩნდეს) გააკეთე ქვემოთ ნაბიჯები.

---

## ნაწილი 1 — Backend Railway-ზე

1. აიტვირთე ეს პროექტი GitHub-ზე (ან მხოლოდ `server/` საქაღალდე ცალკე რეპოდ).
2. Railway → **New Project** → **Deploy from GitHub repo** → აირჩიე რეპო.
3. **Postgres დაამატე:** პროექტში → **New** → **Database** → **Add PostgreSQL**.
   (Railway ავტომატურად ქმნის `DATABASE_URL` ცვლადს და აკავშირებს სერვისს.)
4. Backend სერვისის **Settings**-ში:
   - **Root Directory:** `server`
   - **Start Command:** `npm start` (თუ თავისით ვერ დაინახა)
5. **Variables** ტაბში დარწმუნდი რომ `DATABASE_URL` არსებობს. დაამატე `JWT_SECRET` = გრძელი შემთხვევითი სტრიქონი (რეგისტრაცია/შესვლის უსაფრთხოებისთვის). (სურვილისამებრ დაამატე `CORS_ORIGIN` = შენი Vercel დომენი.)
6. Deploy დასრულების შემდეგ **Settings → Networking → Generate Domain**.
   მიიღებ URL-ს, მაგ: `https://bebos-receptebi-production.up.railway.app`
7. გახსენი ეს URL ბრაუზერში — უნდა დაინახო `{"ok":true,...}`. ბაზა ავტომატურად აივსება 4 ბებიით და 12 რეცეპტით.

## ნაწილი 2 — Frontend Vercel-ზე

1. გახსენი `index.html`, დასაწყისში იპოვე:
   ```js
   const API_BASE = "";
   ```
2. ჩასვი Railway-ს URL:
   ```js
   const API_BASE = "https://bebos-receptebi-production.up.railway.app";
   ```
3. შეინახე და აიტვირთე GitHub-ზე (თუ ჯერ არ გიტვირთავს).
4. Vercel → **Add New → Project** → აირჩიე რეპო.
   - **Framework Preset:** Other
   - **Root Directory:** `./` (root)
   - Build Command / Output — ცარიელი (სტატიკური საიტია)
5. **Deploy.** მიიღებ საიტს, მაგ: `https://bebos-receptebi.vercel.app`

## ✅ შემოწმება
- გახსენი Vercel საიტი — ზემოთ ბანერი **მწვანე** უნდა იყოს: „საერთო რეჟიმი (Cloud)“.
- ატვირთე რეცეპტი ერთ მოწყობილობაზე → გამოჩნდება მეორეზეც.

---

## ლოკალურად ტესტი (სურვილისამებრ)
```bash
cd server
npm install
# დააყენე DATABASE_URL (ლოკალური Postgres ან Railway-ს URL)
DATABASE_URL="postgresql://..." npm start
```
შემდეგ `index.html`-ში `API_BASE = "http://localhost:3000"`.

## ავტორიზაცია (რეგისტრაცია / შესვლა)
- რეცეპტის ატვირთვა და წაშლა შესაძლებელია **მხოლოდ შესვლის შემდეგ**.
- ზედა მარჯვენა კუთხის „შესვლა“ ღილაკით იხსნება ფანჯარა — იქვე შეიძლება რეგისტრაცია.
- Cloud რეჟიმში პაროლი ინახება უსაფრთხოდ (bcrypt hash), სესია — JWT ტოკენით.
- ლოკალურ რეჟიმში (backend-ის გარეშე) ანგარიში ინახება ბრაუზერში — მხოლოდ ტესტისთვის.
- რეცეპტს წაშლა შეუძლია მხოლოდ მის ავტორს (seed რეცეპტები ყველას).

## ადმინის პანელი (`admin.html`)
- გვერდი: `https://შენი-საიტი/admin.html` — აქ ჩანს ყველა დარეგისტრირებული მომხმარებელი და ყველა რეცეპტი იმ ინფოთი, თუ **ვინ ატვირთა**.
- ვინ არის ადმინი: Railway → backend სერვისის **Variables** ტაბში დაამატე `ADMIN_EMAILS` = შენი ელფოსტა (რამდენიმე — მძიმით: `a@x.com,b@y.com`).
- შედი admin.html-ზე იმ ანგარიშით, რომლის ელფოსტაც `ADMIN_EMAILS`-შია. თუ ადმინი ხარ, მთავარ საიტზეც (ზედა კუთხეში, სახელის გვერდით) გამოჩნდება ⚙︎ ბმული.
- ადმინს ნებისმიერი რეცეპტის წაშლა შეუძლია პანელიდან.
- ლოკალურ რეჟიმში (backend-ის გარეშე) `admin.html` აჩვენებს მხოლოდ ამ ბრაუზერში შენახულ მონაცემებს — ტესტისთვის.

## შენიშვნა
- ფოტოების ატვირთვაც შესაძლებელია (ემოჯის ნაცვლად) — Railway volume ან Cloudinary-ით.
